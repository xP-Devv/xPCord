{
  "buildCommand": "npm run build:shared && npm run build:client",
  "outputDirectory": "client/dist/renderer",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
