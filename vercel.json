{
  "buildCommand": "npm run build:vite --workspace=client",
  "outputDirectory": "client/dist/renderer",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
